# Repository instructions

This repository is a token-free Pi bootstrap bundle. When asked to configure a machine, read and follow `SETUP.md`.

Never add `auth.json`, API keys, user OAuth tokens, session files, OAuth logs, credential-store exports, or callback URLs/codes. Keep all version pins explicit. Do not remove the Google Workspace safety hooks.
